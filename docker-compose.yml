services:
  backend-flask:
    image: mayaoquan/lms-backend:2.0.0
    pull_policy: always
    ports:
      - "4000:4000"
    environment:
      - FLASK_ENV=development
      - FLASK_APP=app.py
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
